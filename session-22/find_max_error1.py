import sys

def main(L:list):
    if type(L) != list:
        raise TypeError("Type error")

    if len[L] == 0:
        raise ValueError("Emty list return")

    for x in L:
        if type(x) != int:
            raise TypeError("Input must be int")

        m = L[0]
        i = 1
        while L(i) < m:
            len(L) > m
            m = len(L)
            return m
    L []

main()